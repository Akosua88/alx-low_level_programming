#include "main.h"
#include <stdio.h>

/**
 * main - A program that prints its name
 * argc: Input
 * argv: Input
 * Return: Always (Success)
 *
 */

int main (int argc, char *argv[])

{
	(void) argc;

	printf("%s\n", argv[0]);

	return (0);

}
