#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - A program that prints all single digit numbers of base
 *
 * Return: Always (Success)
 */

int main(void)

{
	int c;

	for (c = '0'; c <= '9'; c++)
	putchar(c);
	putchar('\n')
	return (0);
}
